[phases.setup]
nixPkgs = ["nodejs_22", "pnpm-9_x", "caddy"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["PORT=3000 BASE_PATH=/ pnpm -r --filter './artifacts/krypto-knight' run build"]

[start]
cmd = "caddy run --config /app/Caddyfile --adapter caddyfile"
